# status — 작업 보드

> 마지막 정리: 2026-09-14 (기록-실제 정합 점검)
> **원본 기록처**: 루트 `CLAUDE.md` §9 "지금 위치 / 다음 할 일" 및 각 모듈 README.
> CLAUDE.md는 공개 보류라 깃에 안 올라갑니다 → 여기에 **요약본**을 둡니다.
> 원본이 바뀌면 이 파일도 같이 손봐 주세요(둘이 어긋나면 CLAUDE.md가 맞습니다).

## 지금 하는 일 (진행 중)

- **실시간 수집 파이프라인 구현 준비** (data_pipeline → `raw_market_snapshot`)
  실시간 판별기 설계가 끝나 다음은 원료 수집. **착수 전 확인 필수: KIS 앱키 분리**
  (옛 오딘 라이브와 호출 한도를 공유하면 라이브 매매를 방해할 위험 — 절대 규칙).
  적재 시간 원칙(2026-09-12): **배치성 적재는 전부 밤으로**(일봉 백필·명부 갱신·DART —
  라이브와 시간대 분리. 미국 일봉만 한국 아침 6시 이후). 장중이 불가피한 건 실시간 1분
  수집뿐 → **모의투자 앱키로 시세 조회가 되는지 확인**(되면 실전 키와 한도 분리 해결).
  분봉 이력 출처 확인 결과(2026-09-12, 옛 코드 읽기 전용 조사): 옛 DB에 **지수 분봉은 없음**
  (1분마다 받아 판정만 하고 시세는 버림, 판정도 daily upsert). 있는 것 — `index_daily_ohlc`
  (지수 일봉)·`ml_minute_ohlc`(관심종목 한정 종목 분봉). **채택: 옛 DB 밤 복사**로
  `raw_index_ohlc` 백필(KIS 재호출 0건) + 종목 분봉은 속도 규칙 검증 참고용. 지수 분봉
  이력은 3.0 실시간 수집을 시작해야 쌓임 — 밤 복사는 과거 이력용, 장중 원료는 장중 수집.
  추가 결정(2026-09-12): **옛 오딘이 지수 분봉을 저장하도록 변경**(절대 규칙 1번의 유일한 예외,
  사용자 승인) — 새 표 `index_minute_price`에 이미 받아오는 값만 append(추가 KIS 호출 0건,
  실패 격리, 장 마감 후 배포). **→ 당일 구현·배포 완료**(옛 오딘 세션): PK(date,market,time),
  칼럼 date/time/market/price/change_pct/created_at. 기록은 옛 docs/db-schema.md +
  docs/market-regime.md "부산물 수집" 절 — 3.0 밤 복사 스크립트가 참조할 곳.
  **밤 복사 스크립트는 작성·사용까지 완료**(`data_pipeline/copy_from_old.py`+.cmd, 아래
  "끝난 일" 2026-09-13 "3.0 DB 구축 + 일봉 백필 완료" 참고 — 일봉 998줄 이관에 이미 씀).
  잔여(사람 확인 필요, 아직 확인 여부 근거 없음): ① **월요일(2026-09-14, 오늘) 장중 확인 2건**
  (라이브 정상 / 분봉 누적 ~390행×2)은 옛 오딘 세션 몫. ② **KIS 앱키 분리 확인**(모의투자 키로
  시세 조회가 되는지 — 라이브와 호출 한도 공유 위험). 이 두 확인이 끝나야 실시간 수집
  파이프라인(`raw_market_snapshot`) 착수.
- **GUI 첫 화면 S1(시장 온도) — 디자인 재스킨까지 완료, 커밋 전**
  아래 "끝난 일"에 정리된 대로 v7(사다리+꼬리표+폭 바)→v8(단일 상태값+SUIT)→v9(Supabase
  실데이터 연결)→**v10(Koyfin 레퍼런스 재스킨, 2026-09-14) 완료**까지 진행됨. 국내 탭은
  실데이터, 미국 탭·해외 타일은 여전히 목데이터(pending #9). 중세풍 컨셉 스킨은 보류(pending #10).
  남은 것: 이 작업물 커밋 + 다음 화면(S2, 시장 상세) 설계 착수(아래 "다음 할 일" 참고).

## 다음 할 일 (대기)

- **실시간 판별기 코드 구현**(state_module) — DB 표는 설계 완료, 수집 파이프라인 다음 순서.
- **주 단위 무드(매도 태도 결정)·월 단위 판별 재설계** — 일봉 기반. 실시간이 돌기 시작하면.
- **S1 화면의 미정 항목** — 시장 상세 화면(S2 후보) 정하기.
  (파생 이름표 문구 규칙은 해소됨 — 2026-09-13 상태 어휘 v2로 state_code 10개 고정 +
  label_kr 코드 자동 생성 방식 확정. 아래 "끝난 일" 참고)

> `raw_index_ohlc` 적재·3.0 Supabase 프로젝트 채우기는 완료되어 "끝난 일"로 옮김
> (근거: 아래 2026-09-13 "3.0 DB 구축 + 일봉 백필 완료" 항목).

## 끝난 일

- 2026-08-30 · 폴더 구조 확정 — 모듈 5개 + `db/`, 각 폴더에 쉬운 README.
- 2026-09-02 · `dim_sector`(업종 번호 → 큰 묶음 이름 번역표) 첫 버전 커밋.
- 2026-09-03 · 옛 `market_regime`은 **이식하지 않고 3.0에서 새로 설계**하기로 결정.
- 2026-09-04 · 상태 모듈 1층 "2축 온도계" 체계 확정 · 대상 시장 8개(나라×판) 확정.
- 2026-09-04 · 상태 모듈 1층 후속 설계 확정: 한국 시장 거래시간 사실관계 확인,
  추적 창(08~20시 전체, 지수 없는 구간은 프록시)·프록시 방식(개별 종목 바스켓, ETF 안 버림) 확정.
  (근거: `state_module/README.md` — 인터페이스 초안은 아직 "확정 전 — 논의 중")
- 2026-09-04 · GUI 스택 **pywebview** 결정(`odin_gui/`, `uv run main.py`).
- 2026-09-04 · 화면 작업 철칙 도입 — 화면을 고치면 보고 전에 헤드리스 렌더로 자가검증
  (무검증 납품 사고 이후 생긴 규칙). 절차 원문은 `CLAUDE.md` §9.
- 2026-09-06 · GUI 디자인 시스템 `odin_gui/DESIGN.md` 확립("인더스트리얼 모노 계기판").
- 2026-09-10 · 일간보고에서 칸반 마감 3건(제목 '8'·'9'·'10')이 `pending.md` 보류 안건
  번호였던 착오를 확인 — 실제 작업 아님, 재확인일 2026-09-24로 재확정(자세한 경위는 `log.md`).
- 2026-09-11 · ohmyPM 게시판 논의로 `pending.md` "다시 볼 시점·조건" 칸 표기 규칙 정리
  (칸에는 값 하나만 — 날짜와 조건을 섞거나 "충족(날짜)"를 덧붙이지 않음, 조건 충족 시
  줄을 지우거나 여기 "끝난 일"로 옮김). 연동 파서 오탐 예방 목적, 새 작업 아님.
- 2026-09-12 · **상태 모듈 판정 파라미터 v1 확정** — 방향 3개(20일 이평 위치·기울기·5일 등락률)
  + 열 3개(진폭·속도·거래대금, 시장별 1년 백분위 정규화) + 폭 1개(업종 대장주 21개 상승/하락·역행
  업종). 출력 표준화: 전 모델이 같은 한 줄, 이름표는 파생해 저장, horizon 칸 추가. 내부 통신 DB
  재확인(MCP는 pending #8 유지), 패닉 시 강제 현금화는 폐기(pending #11 신설).
  상세: `state_module/README.md`
- 2026-09-12 · **마트 3단 구조 확정** — 파라미터 계산(공용, `state_params`) → 예측모델별 output
  (`state_market_temp` + model_id, 규칙 판정 = 1호 모델, 기준선도 모델로 등록) → 매매모델이
  예측모델을 구독(model_id). 마트 명부 `dim_model`. 상세: `state_module/README.md` "마트 3단 구조"
- 2026-09-12 · **실시간 판별기 설계 완료 + DB 표 5개 DDL 작성** — 시야 역할 재편(실시간=사다리
  판정 우선 / 주=무드로 매도 태도 결정 / 월=보류). 어휘 = 사다리 7개(대공황~과열)+꼬리표(반등
  시도·테마주상승/하락-{업종}). `db/`에 raw_market_snapshot·dim_basket·state_params·
  state_market_temp·dim_model 추가. 규칙 숫자는 잠정(일봉 검증은 뒤로 — 사용자 지시).
  상세: `state_module/README.md` "실시간 판별기"
- 2026-09-12 · **옛 오딘 지수 분봉 저장(`index_minute_price`) 구현·배포 완료**(옛 오딘 세션 작업,
  절대 규칙 예외 — 상세는 위 "지금 하는 일" 항목). 월요일 장중 확인 2건만 잔여.
- 2026-09-12 · **S1 화면 v7 전환** — output 재설계 반영(실시간 = 사다리 워드+꼬리표+폭 21칸 바+
  지속시간, 주간 = 무드+매도 태도, 월간 제거) + 밀도 보강(지수값·등락률·스파크라인·해외 6시장
  타일 복원) + **폰트 Pretendard 교체**(DESIGN.md 갱신). 헤드리스 검증(콘솔 0건·computed style·
  국내/미국 탭 육안) 통과.
- 2026-09-13 · **상태 어휘 v2 = 단일 상태값 확정**(사용자 지시 — 꼬리표 폐기). 상태 10개를
  우선순위로 하나만 판정(급락과 테마주상승은 공존 안 함 — 테마주상승은 완만한 하락 속 역행).
  `db/state_market_temp.sql`·README 갱신. **S1 v8**: 단일 상태 워드 + 월간 복원 + 폰트 SUIT
  (Pretendard 탈락). 헤드리스 검증 통과.
- 2026-09-13 · **급락(긴급) 조건식 확정 — 합의는 여기까지** — 시야 1차 구분(실시간=당일 매매
  영향, 급락은 긴급 등급 / 주간=매도 태도 무드 + **대공황 같은 국면값**("하루 내려갔다고
  대공황이 아니다" — 대공황은 주간 소속으로 이동) / 월간=보류). **급락 조건: 시장별 각자,
  그 시장 대장주 90% 이상이 각자 −0.5%↓**(표본 ≥15개, 3분 연속. 전원 하락 = 확신도 최고 +
  외생 충격 표지). 실측(옛 DB 210거래일, 계산 버그 1차 실측을 정정한 재측정): 코스피 13일·
  코스닥 13일 전부 실제 폭락일, 오탐 0 — 두 시장 같은 규칙 재사용 확인. 폭락 4일의 원인도
  확인(3월 = 미·이란 전쟁 발발 + 급등 피로, 6-26 = 중동 재격화·환율 1,530원대·금리 우려).
  **나머지 상태들 조건식은 미확정 초안** — 사용자와 하나씩 정할 것(한때 전체를 "확정"으로
  잘못 기록했다가 사용자 지적으로 정정).
- 2026-09-13 · **실시간 어휘 v3 = 5개로 재편** — 실시간은 "오늘의 단면"만: 급락(긴급, 확정) /
  테마주상승-{업종 복수 나열}(하락 비율 70%+ 속 역행, 문턱 미정) / 상승·하락·횡보(**지수** 당일
  등락률 기준 — "어제보다 오르면 상승", 밴드 미정: ±0.3% 후보 실측 첨부). "~장" 국면 단어
  (상승장·하락장·과열·대공황)는 전부 주간으로 이동, 테마주하락 삭제, 반등 시도는 "실시간 상승 +
  주간 하락장" 조합으로 대체. DB `state_sectors`(복수) 컬럼 교체 적용 완료.
  횡보 밴드는 **±0.3%로 확정**, 테마 역행 문턱은 **+1%로 확정**, **급등(surge — 90%가
  각자 +0.5%↑, 실측 11일 전부 V반등일) 추가 확정**(2026-09-13) —
  **실시간 6개 상태 조건식 전부 완성.** 다음 설계: 주간(국면+매도 태도 무드).
- 2026-09-14 · **불장·투매 추가(어휘 8개 확정) + 실시간 상태값 시뮬레이션 적재 완료** —
  대금 기준은 "당일÷20일 평균 비율의 1년 백분위 상위 10%"(절대 임계 기각: 하루 대금 중앙값이
  2년 새 10조→41조 4배 변동). 확정 규칙 8개를 과거 2년(시장별 498일)에 돌려
  `state_market_temp`에 996줄 적재(model rule-temp-rt-v1, basis=eod). 스팟체크 통과:
  03-04 급락(−12.06%)·03-05 급등(+9.63%)·05-28 테마주상승-2차전지·6-23 급락·
  07-14 테마주상승-반도체(지수는 +0.7%인데 대장주 18/21 하락 — 삼성전자 혼자 견인, 폭 기반
  판정의 가치 입증). 주의: eod 하루 1판정 근사·현재 대장주 소급·코스닥 커버리지 평균 16.4/21.
- 2026-09-14 · **S1 v9 — 실데이터 연결 완료**: 국내 탭이 목데이터 대신 Supabase 실데이터를 읽음
  (supabase-js CDN + 읽기 전용 공개 키. state_market_temp 최신 상태·폭 바·대금 배율 +
  raw_index_ohlc 지수값·30일 종가 흐름 + **최근 10거래일 상태 띠** 신규). 상단 "종가 기준" 정직
  표기(장중 1분은 수집 후 LIVE로). 미국 탭·해외 타일은 목데이터 라벨 명시. 헤드리스 검증 통과
  (표시값 = DB값 대조 일치: 코스피 6,909.91 −1.76% — 옛 오딘 세션 보고값과 교차 확인).
  어휘 v4(불장·투매·테마 복수) 반영. ※ 사용자 디자인 불만 지속("보면 볼수록 끔찍") →
  **실제 제품 레퍼런스에서 사용자가 직접 선택** 후 재스킨 예정 — 데이터 연결 구조는 스킨과 무관.
- 2026-09-14 · **디자인 레퍼런스 확정 = Koyfin + S1 v10 재스킨 완료** — 실물 캡처 카탈로그
  8종(토스·Koyfin·Webull·업비트·Npay·TradingView·Yahoo·Robinhood, `odin_gui/design_refs/` —
  타사 화면이라 깃 제외)을 만들어 사용자가 **Koyfin 선택**(와이어·팔레트·폰트 참조 지시).
  v10 = 다크 크롬(상단바+사이드바) + 라이트 캔버스 + 흰 패널(1px 테두리·4px)·표 밀도,
  숫자도 본문체, 데이터색은 한국 관례 유지. DESIGN.md를 "Koyfin 문법"으로 전면 교체.
  **pending #7(팔레트 레퍼런스) 해소 — 보류 대장에서 제거.** 헤드리스 검증 통과
  (축소 스크린샷 육안 오판 1회 → 픽셀 샘플링으로 확정하는 절차 추가).
- 2026-09-13 · **3.0 DB 구축 + 일봉 백필 완료** — Supabase `odin_3.0_db`에 표 6개 생성
  (raw_market_snapshot·dim_basket·state_params·state_market_temp·dim_model·raw_index_ohlc)
  + dim_model 씨앗 3줄(1호 모델+기준선 2개). 옛 DB 일봉 998줄(2024-08-22~2026-09-11) 이관,
  **합계 검산 일치**. 밤 복사 스크립트 `data_pipeline/copy_from_old.py`(+.cmd, .env.example)
  작성 — 분봉 동기화용(월요일부터 의미 있음). .env는 .gitignore 차단 확인(공개 레포).
- 2026-09-12 · "칸반 정리하세요"(2026-09-10 사용자 지시) 반영 확인 — 카드 제목 숫자화
  착오는 09-10·09-11에 이미 정리 완료(위 항목들). 추가로 "다음 할 일"에 걸려 있던
  `skills-lock.json` 정리(가리키던 "사람 확인 필요" 절이 실제로는 없어서 붕 뜬 항목)를
  지금 처리 — 이미 지운 스킬(ui-ux-pro-max) 항목을 lock 파일에서 제거하고 빈 목록으로 정리.

## 아직 커밋 안 된 작업물 (2026-09-14 확인)

깃에 올라가지 않은 채 로컬에만 있는 것들입니다. 잃어버리기 쉬우니 정리 대상:

- 수정됨(M): `.gitignore`, `db/README.md`, `docs/pending.md`, `docs/status.md`, `state_module/README.md`
- 새 파일(??): `data_pipeline/.env.example`, `data_pipeline/copy_from_old.cmd`,
  `data_pipeline/copy_from_old.py`, `db/dim_basket.sql`, `db/dim_model.sql`,
  `db/raw_index_ohlc.sql`, `db/raw_market_snapshot.sql`, `db/state_market_temp.sql`,
  `db/state_params.sql`, `odin_gui/DESIGN.md`, `odin_gui/app/`, `odin_gui/main.py`,
  `odin_gui/pyproject.toml`, `odin_gui/screens/`, `odin_gui/uv.lock`, `skills-lock.json`

## 이 보드를 쓰는 법

- 한 줄 = 한 덩어리 일. 끝나면 지우지 말고 **"끝난 일"로 날짜와 함께 옮깁니다.**
- 결정을 미룬 안건은 여기가 아니라 [pending.md](pending.md)로 보냅니다.
