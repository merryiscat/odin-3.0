# status — 작업 보드

> 마지막 정리: 2026-09-09 (환경 점검 시 자동 생성)
> **원본 기록처**: 루트 `CLAUDE.md` §9 "지금 위치 / 다음 할 일" 및 각 모듈 README.
> CLAUDE.md는 공개 보류라 깃에 안 올라갑니다 → 여기에 **요약본**을 둡니다.
> 원본이 바뀌면 이 파일도 같이 손봐 주세요(둘이 어긋나면 CLAUDE.md가 맞습니다).

## 지금 하는 일 (진행 중)

- **상태 모듈 1층(시장 온도) — 축별 판정 지표 확정**
  2축 온도계(방향: 상승/횡보/하락 × 열: 차분/달아오름/과열) 체계는 2026-09-04에 확정.
  남은 것은 "어떤 숫자를 보고 그 축을 판정할지"를 정하는 일.
  상세: `state_module/README.md`
- **GUI 첫 화면 S1(시장 온도) 시안 다듬기**
  실화면 시안 `odin_gui/app/market-temp.html`(가짜 데이터)로 작업 중. 디자인 방향은
  "자동차 계기판"(2026-09-04 확정), 규칙은 `odin_gui/DESIGN.md`가 단일 기준. v5 적용 완료.

## 다음 할 일 (대기)

- **`raw_index_ohlc`(지수 일봉) 적재 설계** — 1층 판정 지표가 정해지면 바로 이어짐.
- **3.0 전용 새 Supabase 프로젝트(빈 통) 채우기** — 옛 오딘 라이브 DB는 건드리지 않음.
  옛 DB에서 복사할지, KIS/DART로 새로 긁을지는 표마다 판단(대체로 새로 긁는 쪽).
- **S1 화면의 미정 항목** — 파생 이름표 문구 규칙, 시장 상세 화면(S2 후보) 정하기.
- **`skills-lock.json` 정리** — 이미 지운 스킬(ui-ux-pro-max)이 목록에 남아 있음(아래 "사람 확인 필요").

## 끝난 일

- 2026-08-30 · 폴더 구조 확정 — 모듈 5개 + `db/`, 각 폴더에 쉬운 README.
- 2026-09-02 · `dim_sector`(업종 번호 → 큰 묶음 이름 번역표) 첫 버전 커밋.
- 2026-09-03 · 옛 `market_regime`은 **이식하지 않고 3.0에서 새로 설계**하기로 결정.
- 2026-09-04 · 상태 모듈 1층 "2축 온도계" 체계 확정 · 대상 시장 8개(나라×판) 확정.
- 2026-09-04 · GUI 스택 **pywebview** 결정(`odin_gui/`, `uv run main.py`).
- 2026-09-04 · 화면 작업 철칙 도입 — 화면을 고치면 보고 전에 헤드리스 렌더로 자가검증
  (무검증 납품 사고 이후 생긴 규칙). 절차 원문은 `CLAUDE.md` §9.
- 2026-09-06 · GUI 디자인 시스템 `odin_gui/DESIGN.md` 확립("인더스트리얼 모노 계기판").

## 아직 커밋 안 된 작업물 (2026-09-09 확인)

깃에 올라가지 않은 채 로컬에만 있는 것들입니다. 잃어버리기 쉬우니 정리 대상:

- `odin_gui/DESIGN.md`, `odin_gui/main.py`, `odin_gui/app/`, `odin_gui/screens/`,
  `odin_gui/pyproject.toml`, `odin_gui/uv.lock`, `skills-lock.json`
- 수정됨: `state_module/README.md`

## 이 보드를 쓰는 법

- 한 줄 = 한 덩어리 일. 끝나면 지우지 말고 **"끝난 일"로 날짜와 함께 옮깁니다.**
- 결정을 미룬 안건은 여기가 아니라 [pending.md](pending.md)로 보냅니다.
