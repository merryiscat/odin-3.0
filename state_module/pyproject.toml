[project]
name = "odin-state-module"
version = "0.1.0"
description = "오딘 3.0 상태 모듈 — 시장 상태 판정(실시간 판별기 rule-temp-rt-v1)"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",        # Supabase REST
    "python-dotenv>=1.0", # 프로젝트 루트 .env 로딩
]

[dependency-groups]
dev = ["pytest>=8"]
