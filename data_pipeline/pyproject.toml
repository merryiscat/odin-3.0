[project]
name = "odin-data-pipeline"
version = "0.1.0"
description = "오딘 3.0 데이터 파이프라인 — 외부 API에서 원천 수집·적재"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",       # HTTP 호출(외부 API + Supabase REST)
    "python-dotenv>=1.0" # 프로젝트 루트 .env 로딩
]
