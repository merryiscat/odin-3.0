[project]
name = "odin-model-module"
version = "0.1.0"
description = "오딘 3.0 매매모델 모듈 — 1호 모델 rule-trade-v1(매매 리스트 + 시장 온도 → 주문 의도, 가상 계좌)"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",        # Supabase REST (읽기 전용)
    "python-dotenv>=1.0", # 프로젝트 루트 .env 로딩
]

[dependency-groups]
dev = ["pytest>=8"]
